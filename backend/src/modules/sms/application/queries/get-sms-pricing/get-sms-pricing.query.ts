/**
 * Takes no arguments: the price is flat, so it is the same for every caller.
 * The empty class exists because `@nestjs/cqrs` keys handlers by constructor.
 */
export class GetSmsPricingQuery {}
